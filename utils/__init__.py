"""Utility layer for the AI Language Translator application.

This package contains all business-logic helpers that are decoupled from
the Streamlit presentation layer, following a clean-architecture approach:

    components/  -> UI rendering (View)
    utils/       -> Business logic & services (Model / Service layer)
    app.py       -> Orchestration (Controller)
"""
