"""UI component layer for the AI Language Translator application.

Each module in this package renders a self-contained, reusable piece of the
Streamlit interface. Components never talk to external services directly —
they always go through `utils/` so business logic stays testable and UI
stays swappable.
"""
